<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="funding.dao.face.ChatRoomDao">

	
	
	<select id="selectRoomById" resultType="funding.dto.ChatRoomDTO" parameterType="string">
		SELECT
			chatroomid,
			roomname
		FROM chatroom
		WHERE chatroomid = #{chatroomid}
	</select>
	
	<insert id="insertChatRoom" parameterType="funding.dto.ChatRoomDTO" parameterType="string">
		INSERT INTO chatroom ( chatroomid, roomname )
		VALUES ( #{chatroomid}, #{roomname} )
	</insert>